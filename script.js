/* Open when someone clicks on the span element */
function openNav(anID) {
    document.getElementById(anID).style.width = "100%";
  }
  
  /* Close when someone clicks on the "x" symbol inside the overlay */
  function closeNav(anID) {
    document.getElementById(anID).style.width = "0%";
  } 